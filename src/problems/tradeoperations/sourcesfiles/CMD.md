```declarative
open -a Docker
```

```declarative
docker start mysql;
```

```declarative
docker exec -it mysql mysql -u root -p
```

```declarative
docker exec -it mysql_unittests mysql -u root -p
```
