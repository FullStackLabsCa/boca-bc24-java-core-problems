package problems.string.advanced.calculator;

class Node {
    String data;  // Use String for a sequence of characters
    Node next;

    Node(String data) {
        this.data = data;
        this.next = null;
    }
}