<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE hibernate-configuration PUBLIC "-//Hibernate/Hibernate Configuration DTD 3.0//EN" "http://hibernate.org/dtd/hibernate-configuration-3.0.dtd">
<hibernate-configuration>
    <session-factory>
        <!-- JDBC Database connection settings -->
        <property name="hibernate.connection.driver_class">com.mysql.cj.jdbc.Driver</property>
        <property name="hibernate.connection.url">jdbc:mysql://localhost:3306/School</property>
        <property name="hibernate.connection.username">root</property>
        <property name="hibernate.connection.password">password123</property>

        <!-- HikariCP Connection Pool Settings -->
        <property name="hibernate.hikari.minimumIdle">5</property>
        <property name="hibernate.hikari.maximumPoolSize">20</property>
        <property name="hibernate.hikari.idleTimeout">300000</property>
        <property name="hibernate.hikari.maxLifetime">1800000</property>
        <property name="hibernate.hikari.connectionTimeout">30000</property>
        <property name="hibernate.hikari.poolName">HibernateHikariCP</property>

        <!-- SQL Dialect -->
        <!-- <property name="hibernate.dialect">org.hibernate.dialect.MySQLDialect</property> -->

        <!-- Echo all executed SQL to stdout -->
        <property name="hibernate.show_sql">false</property>

        <!-- Format SQL nicely -->
        <property name="hibernate.format_sql">false</property>

        <!-- Automatically create/update tables based on entity annotations -->
        <property name="hibernate.hbm2ddl.auto">update</property>

        <!-- Enable auto-detection of annotated classes (option-2)
        <property name="hibernate.archive.autodetection">class,hbm</property> -->

        <!--         Package to scan for entities-->
        <mapping class= "io.reactivestax.Course" />
        <mapping class= "io.reactivestax.Student" />
        <mapping class= "io.reactivestax.Enrollment" />
        <mapping class= "io.reactivestax.Grades" />





    </session-factory>
</hibernate-configuration>